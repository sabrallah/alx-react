import { fromJS } from 'immutable';

<<<<<<<<<<<<<  ✨ Codeium AI Suggestion  >>>>>>>>>>>>>>
+/**
+ * Creates an immutable representation of the input object.
+ *
+ * @param {Object} object - The input object to be converted to an immutable representation.
+ * @return {Immutable} - The immutable representation of the input object.
+ */
<<<<<  bot-cab9536f-1ec0-475e-ba96-66bf3a549636  >>>>>
export default function getImmutableObject(object) {
  return fromJS(object);
}
